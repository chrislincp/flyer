import { StyleSheet } from 'react-native'

const detailCardStyle = StyleSheet.create({
  footer: { flexDirection: 'row', justifyContent: 'center', alignItems: 'center', height: 40 },
})

export { detailCardStyle }
